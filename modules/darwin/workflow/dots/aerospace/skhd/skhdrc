####################
# Project Repo: https://github.com/koekeishiya/skhd
# Reload SKHD: `skhd -r`
# Test Keys: `skhd -o`
#
# Known Special Key Chars:
# 0x18 = plus/equal
# 0x1B = minus/underscore 
# 0x24 = enter
# 0x2B = comma
# 0x2C = slash
# 0x2F = period
# 0x32 = grave (tilde)
# 0x37 = CMD (gui)
#
# Shortcut Documentation:
#
# Application Control:
#   HYPER - B         : Browser
#   HYPER - C         : Claude
#   HYPER - M         : Music (Spotify)
#   HYPER - O         : Obsidian
#   HYPER - P         : Spotify play/pause
#   HYPER - S         : Signal
#   HYPER - T         : Terminal
#   HYPER - Space     : Launcher (Raycast)
#
# Window Sizing:
#   CMD + ALT - +     : Resize larger
#   CMD + ALT - -     : Resize smaller
#   CMD + ALT - Enter : Balance equal sizes
#
# Window Layout:
#   CMD + ALT - R     : Reset (flatten workspace tree)
#   CMD + ALT - S     : Splits / switch layout
#   CMD + ALT - M     : Maximise (fullscreen)
#   CMD + ALT - ,     : Preset floating 1620p
#   CMD + ALT - .     : Preset floating 1080p
#   CMD + ALT - /     : Tiling / toggle
####################


#################### Application Control ####################

# Override CMD - Q, close all tabs of active window, not kill all instances
cmd - q [
  "Floorp": skhd -k "cmd + shift - w"
  "Firefox": skhd -k "cmd + shift - w"
  "Google Chrome": skhd -k "cmd + shift - w"
  "Brave": skhd -k "cmd + shift - w"
] 

# Application Launching
cmd + ctrl + alt + shift - b    : open -na $BROWSER
cmd + ctrl + alt + shift - c    : open -a Claude
cmd + ctrl + alt + shift - m    : open -a Spotify
cmd + ctrl + alt + shift - o    : open -a Obsidian
cmd + ctrl + alt + shift - p    : osascript -e 'tell application "Spotify" to playpause'
cmd + ctrl + alt + shift - s    : open -a Signal
cmd + ctrl + alt + shift - t    : open -na $TERMINAL
cmd + ctrl + alt + shift - 0x31 : open -a Raycast

#################### Focus & Navigation ####################

# Window Focus
cmd - h : aerospace focus left
cmd - j : aerospace focus down
cmd - k : aerospace focus up
cmd - l : aerospace focus right 

# Monitor & Workspace Navigation
alt - tab : aerospace focus-back-and-forth
cmd - tab : aerospace focus-monitor --wrap-around next

#################### Window Movement ####################

# Move Windows
cmd + shift - h : aerospace move left
cmd + shift - j : aerospace move down
cmd + shift - k : aerospace move up
cmd + shift - l : aerospace move right 

# Join Windows
cmd + shift + alt - h : aerospace join-with left mode main
cmd + shift + alt - j : aerospace join-with down mode main
cmd + shift + alt - k : aerospace join-with up mode main
cmd + shift + alt - l : aerospace join-with right  mode main

#################### Window Layout & Sizing ####################

# Window Sizing
cmd + alt - 0x18 : aerospace resize smart +480
cmd + alt - 0x1B : aerospace resize smart -480
cmd + alt - 0x24 : aerospace balance-sizes

# Layout Management
cmd + alt - r    : aerospace flatten-workspace-tree
cmd + alt - s    : aerospace layout tiles horizontal vertical
cmd + alt - m    : aerospace fullscreen
cmd + alt - 0x2B : \
  aerospace layout floating; \
  osascript -e 'tell application "System Events" to tell (first process whose frontmost is true) to set position of first window to {480, 0}'; \
  osascript -e 'tell application "System Events" to tell (first process whose frontmost is true) to set size of first window to {2880, 1620}';
cmd + alt - 0x2F : \
  aerospace layout floating; \
  osascript -e 'tell application "System Events" to tell (first process whose frontmost is true) to set position of first window to {960, 270}'; \
  osascript -e 'tell application "System Events" to tell (first process whose frontmost is true) to set size of first window to {1920, 1080}';
cmd + alt - 0x2C : aerospace layout floating tiling
